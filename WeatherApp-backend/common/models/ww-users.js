'use strict';

module.exports = function(Wwusers) {
    // Wwusers.observe('after save', function(ctx, next) {
    //     if(ctx.isNewInstance === true) {
    //         var instance = ctx.instance;
    //         instance.createAccessToken(1209600000, function(err, res) {
    //             if(err === null) ctx.instance["token"] = res.id;
    //             next();
    //         });
    //     } else {
    //         next();
    //     }
    // });
};
